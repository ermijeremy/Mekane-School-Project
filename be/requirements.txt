# Web framework
fastapi
uvicorn[standard]

# App DB access (async, psycopg 3)
psycopg[binary]
psycopg_pool

# Auth / passwords
passlib
bcrypt==4.0.1  # passlib 1.7.4 is incompatible with bcrypt >= 4.1
python-jose[cryptography]
python-multipart

# Seed script only (sync, psycopg 2)
psycopg2-binary
